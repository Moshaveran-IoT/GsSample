# 🚀 راهنمای سریع اجرای تست

## تنظیمات انجام شده ✅

- ✅ Connection String به‌روزرسانی شد: `localhost`, User: `ETL`, Password: `123456`
- ✅ دیتابیس به صورت خودکار ایجاد می‌شود
- ✅ جدول Persons به صورت خودکار ایجاد می‌شود
- ✅ 10 رکورد نمونه به صورت خودکار insert می‌شود

## اجرای تست

### روش 1: اجرای مستقیم (توصیه می‌شود)

```bash
cd API
dotnet run
```

### روش 2: Build و سپس Run

```bash
dotnet build
cd API
dotnet run
```

## چه اتفاقی می‌افتد؟

1. ✅ **اتصال به دیتابیس** - تست اتصال با اطلاعات جدید
2. ✅ **ایجاد دیتابیس** - اگر وجود نداشته باشد
3. ✅ **ایجاد جدول** - اگر وجود نداشته باشد
4. ✅ **درج داده‌های نمونه** - 10 رکورد فارسی
5. ✅ **تست Repository** - تمام عملیات CRUD
6. ✅ **تست MediatR Handlers** - تمام Commands و Queries
7. ✅ **راه‌اندازی API** - API آماده استفاده

## داده‌های نمونه

10 رکورد با نام‌های فارسی:
- علی محمدی
- فاطمه احمدی
- محمد رضایی
- زهرا کریمی
- حسین نوری
- مریم حسینی
- رضا موسوی
- سارا جعفری
- امیر کاظمی
- نرگس صادقی

## بررسی در SQL Server

```sql
-- مشاهده همه داده‌ها
SELECT * FROM Persons;

-- شمارش
SELECT COUNT(*) FROM Persons;
```

## نکات مهم

- اگر دیتابیس از قبل وجود داشته باشد، فقط جدول ایجاد می‌شود
- اگر جدول از قبل داده داشته باشد، داده‌های نمونه insert نمی‌شوند
- داده‌های تست (نه نمونه) بعد از تست حذف می‌شوند
- داده‌های نمونه باقی می‌مانند

